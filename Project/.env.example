# Codebase Assistant — Docker / local secrets template
# Copy to .env and fill in values (never commit .env):
#   cp .env.example .env

# Required for LLM features (Analysis / Docs / Testing via OpenRouter)
OPENROUTER_API_KEY=

# Optional overrides (Compose sets these to /data/... inside the container)
# Local default when unset: {temp}/codebase_assistant_streamlit/chroma
# (shared by Streamlit/worker and CLI via CHROMA_PERSIST_DIR / Config.load)
# CODEBASE_ASSISTANT_DATA_DIR=/data
# CHROMA_PERSIST_DIR=/data/chroma
# MEMORY_STORE_PATH=/data/memory
# RUN_HISTORY_PATH=/data/history/ui_run_history.jsonl

# Evidence grounding (default off — keep model/static findings without scrubbing)
# GROUNDING_ENABLED=false

# Warm Streamlit worker (localhost HTTP; not published outside the container)
# CA_WORKER_URL=http://127.0.0.1:8765
# CA_WORKER_PORT=8765
# CA_WORKER_HOST=127.0.0.1
