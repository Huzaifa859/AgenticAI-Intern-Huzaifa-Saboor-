# Runtime dependencies for the Docker / Streamlit deployment.
# Analysis, Documentation, and Testing agents + warm worker + RAG.
# Excludes Jupyter and MCP (not used in this Compose stack).
# Keep pytest/pytest-cov: Testing Agent executes generated suites in-container.

# --- Structured output -------------------------------------------------
pydantic>=2.0

# --- Configuration -----------------------------------------------------
python-dotenv>=1.0

# --- Ingestion ---------------------------------------------------------
GitPython>=3.1

# --- RAG ---------------------------------------------------------------
chromadb>=0.4
sentence-transformers>=2.2
# jsonschema (via chromadb) prefers rfc3987 for URI checks.
rfc3987>=1.3.8

# --- Static analysis ---------------------------------------------------
pyflakes>=3.0

# --- Model access ------------------------------------------------------
requests>=2.31

# --- Web UI ------------------------------------------------------------
streamlit>=1.32

# --- Testing Agent runtime ---------------------------------------------
# Required at runtime to run/measure generated pytest suites (not only CI).
pytest>=7.4
pytest-cov>=4.1
